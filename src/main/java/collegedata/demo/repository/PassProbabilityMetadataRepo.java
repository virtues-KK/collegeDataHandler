package collegedata.demo.repository;

import collegedata.demo.entity.PassProbabilityMetadata;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PassProbabilityMetadataRepo extends JpaRepository<PassProbabilityMetadata,Long> {
}
