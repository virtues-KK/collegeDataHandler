package collegedata.demo.repository;

import collegedata.demo.entity.PassProbabilityMajorMetadata;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PassProbabilityMajorMetadataRepo extends JpaRepository<PassProbabilityMajorMetadata,Long> {
}
